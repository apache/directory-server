<?xml version="1.0" encoding="ISO-8859-1"?>
<project>
    <extend>${basedir}/../project.xml</extend>
    <groupId>ldapd-server</groupId>
    <id>oid-registry</id>
  
    <name>LDAPd Server OID Registry</name>
    <package>ldapd.server.schema</package>
    <currentVersion>SNAPSHOT</currentVersion>
    <inceptionYear>2002</inceptionYear>
      
    <shortDescription>LDAPd Server Object Identifier Registry</shortDescription>

    <description>
        The OID Registry manages OID mappings to names.  It is a memory 
        resident service that populated at various stages by schema object 
        managers.  The bootstrap managers and the solid state managers all 
        deposite OID and name information into the registry to enable quick
        bidirectional name to OID lookups.  Other constructs indirectly
        leverage the OID registry like normalizers and comparators.
    </description>
      
    <dependencies>
        <dependency>
            <groupId>ldapd-server</groupId>
            <artifactId>schema-api</artifactId>
            <version>SNAPSHOT</version>
            <url>http://ldapd.sourceforege.net/ldapd-server</url>
        </dependency>
        
        <dependency>
            <groupId>avalon-framework</groupId>
            <artifactId>avalon-framework-api</artifactId>
            <version>4.1.5</version>
            <url>http://avalon.apache.org/framework</url>
        </dependency>
    </dependencies>
</project>

