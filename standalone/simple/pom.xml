<?xml version="1.0" encoding="ISO-8859-1"?>
<project>
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.apache.directory.server</groupId>
    <artifactId>build</artifactId>
    <version>0.9.4-SNAPSHOT</version>
  </parent>
  <artifactId>build</artifactId>
  <groupId>org.apache.directory.server.standalone.simple</groupId>
  <name>simple-build</name>
  <packaging>pom</packaging>  
  <dependencies>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>nlog4j</artifactId>
      <version>1.2.19</version>
      <scope>provided</scope>
    </dependency>  
    <dependency>
      <groupId>org.apache.directory.server.protocols</groupId>
      <artifactId>org.apache.directory.protocol.changepw</artifactId>
      <version>0.7-SNAPSHOT</version>
    </dependency>  
    <dependency>
      <groupId>org.apache.directory.server.protocols</groupId>
      <artifactId>org.apache.directory.protocol.ntp</artifactId>
      <version>0.5-SNAPSHOT</version>
    </dependency>  
    <dependency>
      <groupId>org.apache.directory.server.protocols</groupId>
      <artifactId>org.apache.directory.protocol.kerberos</artifactId>
      <version>0.7-SNAPSHOT</version>
    </dependency>  
    <dependency>
      <groupId>org.apache.directory.server.protocols</groupId>
      <artifactId>org.apache.directory.protocol.ldap</artifactId>
      <version>0.9.4-SNAPSHOT</version>
    </dependency>  
    <dependency>
      <groupId>org.apache.directory.server</groupId>
      <artifactId>org.apache.ldap.server.core</artifactId>
      <version>0.9.4-SNAPSHOT</version>
    </dependency>  
  </dependencies>

  <profiles>
    <profile>
      <id>jdk1.5</id>
      <activation>
        <jdk>1.5</jdk>
      </activation>
      <modules>
        <module>jndi</module>
        <module>main</module>
        <module>ssl</module>
        <module>unit</module>
      </modules>
    </profile>

    <profile>
      <id>jdk1.4</id>
      <activation>
        <jdk>1.4</jdk>
      </activation>
      <modules>
        <module>jndi</module>
        <module>main</module>
        <module>unit</module>
      </modules>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-antrun-plugin</artifactId>
            <executions>
              <execution>
                <phase>validate</phase>
                <configuration>
                  <tasks>
                    <echo>
====================================================================
                       W A R N I N G
                       -------------

Build with 1.4 jvm will not include modules:

 o standalone-simple-ssl

Use JDK 1.5 to make sure these modules build.
====================================================================
                    </echo>
                  </tasks>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>

